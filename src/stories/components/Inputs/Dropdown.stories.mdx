import { Meta, Story, Canvas,  ArgsTable, Preview  } from '@storybook/addon-docs/blocks';


import {backgroundThemes} from "../../Themes"
import Dropdown from "./dropdown"
import {TexturePropertyProvider} from "../../../context/texturePropertyContext"
import "../../../styles/index.css"

import {TextureTypes} from "../../../TestData/DefaultData"

<Meta title="Components/Inputs/Dropdown" component={Dropdown} />

# Dropdown 

It always "rememebrs" the position of the last selected element, and shifts the list to appear exactly like that. 

<Preview>
    <Story name="default">
        <div className="w_prop h-40 flex items-center">
            <Dropdown list={TextureTypes} type="solid"  />
        </div >
    </Story>
</Preview>

